import { createSlice } from "@reduxjs/toolkit";


const initialState = []

const DeleteEditSlice = createSlice({
    name: "DeleteEditSlice",
    initialState,
    reducers:{},
});

export default DeleteEditSlice.reducer;